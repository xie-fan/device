# 术语

玩具设备模拟器的领域词汇。只放词义，不放实现——契约在 `docs/toy-device-simulator/`。

## 音频格式的四条轴

「格式」在本项目里同时指四件不同的事，混用是最常见的误解来源。

**设备线上格式**（`audio.format`）
一台设备在 WebSocket 上收发音频用的格式，写进每个 `AudioHeader` 的 `AudioFormat` 字段。
取值 `pcm | wav | mp3 | amr | aac`。它是**设备的属性**，不是某段音频的属性——同一台设备
发出去的每一帧都是这个格式，与音频素材原本是什么无关。

**资产格式**（音频库条目的 `format`）
导进音频库的那个文件本身的格式。与设备线上格式**无关且常常不同**：资产格式不符时，
送出前会转出一份该设备规格的派生副本。「导入 mp3 喂给 amr 设备」是正常操作，不是错误。

**下行实际格式**（`down_format`）
服务端这一轮实际回过来的音频格式，取自首个 TTS 帧的帧头，记在 `turn.json` 里。
不能假定它等于设备线上格式——`down_format` 为空表示这一轮压根没有下行音频。

**编码 vs 容器**
`amr`、`aac` 这类名字在协议里同时指编码和承载它的容器形态，而形态是可变的：
同样是 AMR 编码，「一个完整文件」与「每包各带一个存储头的多个包」是不同的字节形态。
说「设备是 amr 格式」没有指明形态；下行的形态由服务端按固件的消费方式决定。

## 主体

**设备**（device）
配置树上的一份设备配置，由 `device_id` 标识。跨启停长期存在。

**实例**（instance）
设备的**一次运行**，由 `instance_id` 标识——准确说是「一个 manager 进程里的这台设备」：
`POST /devices` 时分配一次，stop/start 只换 `conn_generation`，manager 重启才换新实例。
查历史必须带 `instance_id`，因为不同实例的录音与事件游标互不相通。

一个实例结束后有两种去向，别混：**墓碑**（tombstone）是「设备被删了」，整份事件日志
留在内存，TTL 内可查；**盘上历史**是「manager 重启了」，设备还好好的，只是这一次运行
结束了，录音与 `events.jsonl` 留在盘上，没有 TTL。两者都只读，但只有前者有过期。

**轮**（turn）
一次完整的「送一段上行 → 服务端回复 → 终态」。一轮占用设备的说话槽，
终态原因（`turn_end_reason`）说明它是怎么结束的：正常收尾、被打断、超时。
**下行静默也是一种终态**，不代表失败。agent 的一次工作就是跑一轮，不为这个再造词。

**agent**
按 `device_id` 引用已配好的设备、跑完当场断言的调用方。它和人共用同一个 manager——
人在调试台上改的当前值，agent 下一发就会踩到。

**判语**（`verdict`）
一轮结束后对终止表的机械分类，说的是协议层发生了什么（回了音、没回话、出错），
不是「这次测试通过没通过」。字段名保持英文 `verdict`。

**上行 / 下行**
上行 = 模拟器发给服务端（模拟设备录的音）；下行 = 服务端发回来（TTS 回复）。
两个方向的格式各自记录，不是同一件事。

**配置树**（registry）
环境 → 厂商 → 设备类型 → 设备的四层挂靠关系。厂商与设备类型各有名称与**简称**，
上线报文和环境 url 占位符里用的都是简称，不是名称。

**音频库**（asset library）
跨重启存活的音频素材库。库里的东西叫**资产**（asset），可以喂给任意设备。

**租约**（lease）
一次 `simctl run` 对一台设备的独占声明，跑之前占、跑完还，用来保证两个并发 run
不会撞到同一台。它是 **run 之间的协作锁，不是设备的排他锁**——人在调试台上的操作
一概不受它影响。只活在内存里，manager 重启即失效。别和 core 的**槽**（slot）混淆：
槽保护的是一台设备上的一轮，租约保护的是一次 run 的整段占用。

## 对端

**真实服务端**
被模拟器联调的那个语音服务。它是格式行为的唯一权威：下行格式跟随上行格式，
但只有部分格式有下行编码契约。哪些有见 `docs/toy-device-simulator/phase6.md` 的对照表。

**echosrv**
本地验收桩，不是服务端的复刻。它模仿真实服务端的**字节形态**（格式、分包），
不模仿其业务语义（ASR、播放水位、鉴权）。它的行为不能当作真实服务端行为的证据。
