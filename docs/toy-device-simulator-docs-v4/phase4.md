# Phase 4 按需增强（v4）

每项排期前补范围、依赖、验收。

- wake 词与交叉场景
- 指标面板
- 会话回放 diff
- 更多格式转码
- 浏览器麦克风
- SQLite 历史
- `server_observed_drop` 探针
- 高并发压测
- MQTT 对照
- **`queue` 并发策略**（须定义最大长度、入队超时、取消时 Stage=3 与原因字段规则）
- `dup_uuid` / 错误 Stage 的基线实证后再写入 drop 矩阵
- 切换到其他 `ai-creates-wealth` 树时重写 `bad_seq` 与矩阵
